* Debug
  - Create a macro that shows the __FILE__ __FUNCTION__ __LINE__ macros to the debug output

* XML Plugin
  - Design a XML pattern for scenario, sensor and robot plugins (done)
  - XML Parser (under development)

* Interpreter
  - Choose which interpreter to use as default robotqt language
  - Embed that interpreter

* Simulation engine
  - Implement the simulation engine on top of QGraphicsView Framework, if possible
  - if not possible, find out another solution

* State machine
  - Implement State machine using QSateMachine Framework to handle all simulation process

* Handle setting
  - Design which settings should be saved and restored
  - Implement using QSettings (there is a skeleton already)